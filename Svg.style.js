import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  webImage: {
    width: 500,
    height: 500
  },
  imageContainer: {
    display: 'flex',
    width: '100%',
    height: '100%',
    maxWidth: '100%',
    maxHeight: '100%',
    marginTop: 30,
    marginBottom: 30
  }
})

export default styles